import { SearchParticipantsPipe } from './search-participants.pipe';

describe('SearchParticipantsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchParticipantsPipe();
    expect(pipe).toBeTruthy();
  });
});
